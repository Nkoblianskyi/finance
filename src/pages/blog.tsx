import { Hero } from "@/components/hero/hero";
import { Layout } from "@/layouts/main_layout";

const Blog = ( {}) => {
    return (
        <div className="container">
            <Layout>
                <Hero/>
            </Layout>
        </div>
    )
}

export default Blog;